# 251121

## 9:29

```cmd
# windows
winver

python --version

nvidia-smi
```

```cmd
# macOS
sw_vers
python3 --version
system_perfiler SPDisplayDataType
```

```cmd
# linux
cat /etc/os-release
python3 --version
nvidia-smi
nvcc --version
```

```cmd
conda create -n pytorch-env python=3.10
conda activate pytorch-env
conda install pytorch torchvision torchaudio cpunoly -c pytorch
```

```bash
git clone --recursive https://github.com/pytorch/pytorch
cd pytorch
python setup.py install
```

```python
import torch
print(torch.__version__)
print(torch.cuda.is_available())
```

```python
import torch
x = torch.rand(5, 3)
print(x)
```

