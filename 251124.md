# 251124

## 14:19

tensor

```python
import torch
data = [[1, 2], [3, 4]]
torch.tensor(data)
size = (2, 3)
torch.zeros(size)
torch.ones(size)
torch.empty(size)
torch.rand(size)
torch.randn(size)
start, end, step = 0, 10, 2
torch.arange(start, end, step)
start, end, steps = 0, 10, 5
torch.linspace(start, end, steps)
size = 3
torch.eye(size)
ndarray = np.array([1, 2, 3])
torch.from_numpy(ndarray)
```

```python
import torch
tensor_2d = torch.rand((2, 3))
print(tensor_2d.shape)

tensor_3d = torch.stack([tensor_2d, tensor_2d + 10, tensor_2d + 20])
```

```python
tensor.shape
tensor.size()
tensor.dtype
tensor.device
tensor.dim()
tensor.requires_grad
tensor.numel()
tensor.is_cuda
tensor.T
tensor.item()
tensor.is_continuous()
```

```python
z = x + y
z = torch.matmul(x, y)
z = torch.dot(x, y)
z = torch.sum(x)
z = torch.mean(x)
z = torch.max(x)
z = torch.min(x)
z = torch.argmax(x, dim=1)
z = torch.softmax(x, dim=1)
```

```python
shape = (3, 4)
x.view(shape)
x.reshape(shape)
x.t()
dim = 0
x.unsqueeze(dim)
x.squeeze(dim)
torch.cat((x, y), dim=1)
```